Prompt:
The below prompt and BluePrint need to create MTPL file for each BOM.   \BOM\ is a symbol , the options are : C

Below commnads are relevant for \TEMPLATE\ = VminTC based on VminTC from BluePrint:

Foreach \SINGLEFLOW\ and \MULTIFLOW\ create VminTC instance per \CONTENT\ at the same order in the \CONTENT\ column , if there is NA in specific cell in the table for these columns ignore this cell and don't generate test,if else generate test  
Foreach \VMAX_SINGLE\ and \VMAX_MULTI\ create VminTC instance per \CONTENT\ at the same order in the \CONTENT\ column , if there is NA in specific cell in the table for these columns ignore this cell and don't generate test,if else generate test
Foreach \FMIN_SINGLE\ create VminTC instance per \CONTENT\ at the same order in the \CONTENT\ column ,if there is NA in specific cell in the table for this column ignore this cell and don't generate test,if else generate test
Number of test that must be generated is depending on 

Create Unique basenumber for each instance
Create unique full bin (8 digits) following these rules - Port0,2,3  start with 45 next 2 digits won't be 19, Port 4 starts with 9745, Port 5 Starts with 4519

Patlist Parameter value must be in lowercase for all tests

If \SINGLEFLOW\ is 'F2XCCF' or 'F3XCCF' or 'F4XCCF'
The tests should replace THE following paramter with this value:
EndVoltageLimits = __shared__::FlowMatrixSingular.CCF_HIGH_SEARCH_VALUE;
